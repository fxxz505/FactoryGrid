# 资源说明

当前版本不使用外部图片素材。机器与传送带已经改为 Canvas 和 CSS 绘制的简洁几何体，以便贴近异形工厂式的抽象机器语言，并保证格子之间可以无缝连接。

历史上曾下载过 Kenney Factory Kit 3.0 到 `public/assets/` 作为实验素材，但当前运行代码不引用 `/assets/factory-kit/`，E2E 会验证页面没有发起该目录下的资源请求。
